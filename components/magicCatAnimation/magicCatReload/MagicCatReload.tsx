export const MagicCatReload = () => {
  // Note: Preload links removed as they don't work with App Router client components
  // and we no longer have NEXT_PUBLIC_SUPABASE_URL
  // Images will still load, just without preloading
  return null;
};
